# Tesseract

This project visualizes a 4D hypercube (tesseract) by projecting it into 3D space using mathematical projection, then rendering it in the browser using Three.js.

## Installation

```bash
git clone https://github.com/SilentSword123456/silentlab.git
cd silentlab
npm install
npm run dev
